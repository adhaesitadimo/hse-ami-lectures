% !TEX encoding = Windows Cyrillic
\documentclass[12pt]{article}
\usepackage[russian]{babel}
\usepackage{amsmath,amsfonts,amssymb,amsthm,mathtools} % AMS
\topmargin=-2cm
\textheight=23,5cm
\oddsidemargin=-35pt
\textwidth=18cm
\usepackage{color}
\definecolor{light-blue}{rgb}{0.8,0.85,1}
\usepackage{graphicx}

\begin{document}

В данной лекции все рассмотренные функции являются всюду определенными

SLP - straight line programmes. Схема - функция, заданная последовательность присваиваний. Рассматриваем такую функцию, определенную для булевых значений. $f: {0, 1}^n -> {0, 1}$.

Булева схема в базисе B (где B - набор булевых функций) - последовательность функций $x_1, x_2, x_3... x_n, x_n+1 = s_1, x_i = s_L-n. s_j := g(s_{i_1},  ... , s_{i_r}), g \in B, i_\mathalpha < j$

Стандартный базис: {отрицание конъюнкция дизъюнкция}

Схема вычислений $x_L(x_1 ... x_n)$



ПРИМЕР 1

Cтандартный базис.
$x_1, ... x_n, s_1 := \not x_1, s_2 := \not x_2, s_3 := x_1 \and s_2, s_4 = x_2 \and s_1; s_5 = s_3 \or s_4$.

 (1)
 
 $x_2 = 0
 s_5 = x_1$
  
 $x_2 = 1
 s_5 = \not x_1$
 
 Результатом является сложение по модулю 2 (1, если значения x_1 и x_2 разные) - xor.
 
 ПРИМЕР 2
 
 Составим схему которая является xor нескольких переменных. Индуктивное доказательство существования такой функции:
  
$ x_1 \xor x_2 \xor ... \xor x_n-1 \xor x_n = (x_1 \xor x_2 \xor ... \xor x_n-1) \xor x_n$
 
 (2)
 
 ПРИМЕР 3
 
 Дизъюнкция n переменных - аналогично, по индукции.
 
 (3)
 
 Анализ схем
 
 Анализ базисов: все ли функции можно выразить через схему в данном базисе?
 
 Определение
 Полный базис: Базис B - полный, если любую булеву функцию можно вычислить схемой в базисе B.
 
 
 
 Теорема: Стандартный базис - полный.
 
 Доказательство:
 
 Вспомним, что ДНФ - это дизъюнкция конъюнктов литералов. Построим схему ДНФ.
$x_1,... x_n, \not x_1, ... \not x_n, c_1, ...  c_n, D$, где $c_j$ - конъюнкция литералов, D - дизъюнкция. Данный порядок действий соответствует определению ДНФ, следовательно ДНФ представима в виде схемы и любая функция представима в виде ДНФ. что доказано ранее. (Note: $0 = x \and \not x, 1 = x \or \not x$)

Лемма:
$B_1$ - полный.
$\forall f \in B_1$ вычисляется схемой в базисе $B_2$.
Тогда $B_2$ - полный.

Докво:
$(B_1) x_1,... x_n,s_j := $вычисление $f, F(..)$
$(B_2) 			   $вычисление $f, F(..)$

Заметим, что если стандартный базис выразим через некоторые функции, то данные функции будут составлять полный базис.


Следствие 1:

Полнота базиса Жегалкина ${1, x_1 \and x_2, x_1 \xor x_2}$.

Любая формула является схемой.
$\not  x_1 = 1 \xor x_1$

$x_1 \or x_2 = x_1 \xor x_2 \xor (x_1 \and x_2)$
||
$\not (\not x_1 \and \not x_2)$
||
$1 \xor (1 \xor x_1) \and (1 \xor x_2)$




Формула - частный случай схемы. Не любая схема формула.

Пример:
$x_1 \xor x_2 = x_1 \and \not x_2 \or \not x_1 \and x_2 = F
(x_1 \xor x_2) \xor x_3 = (F)  \and \not x_3 \or \not F \and x_3$


Следствие 2:

Полнота базиса {$\not, \and$}.

Докво
$x \or y = \not (\not x \and \not y)$

Доказательство неполноты базиса.
Монотонный базис {$\or, \and$} - неполный
$(x_1 \or x_2,
x_1 \and x_2)$ - монотонный

Определение $f:={0, 1}^n -> {0, 1}$ - монотонная, если
(4)

Утверждение: 
Схема в монотонном базисе вычисляет монотонную функцию.
$x_1, ..., x_n, s_1...s_L, s_L+1
s_L+1:=f(s_{i_1}... s_{i_r})$

(5)

















\end{document}